<%- include('partials/header')-%>
    <h1>Home</h1>
    <p><%= startingContent%></p>
    <!-- <% for(let i = 0;i<pMessage.length;i++){ %>
    <h1><%= pMessage[i].title %></h1>
    <p><%=  pMessage[i].body  %></p>
    <%}%> -->


    <%pMessage.forEach(function(post){%>
        <h1>
            <%= post.title %>
        </h1>
        <p>
            <%= post.content.substring(0,100)+"..." %><a href="/posts/<%= post.title %>">Read more</a>
        </p>
        
        <%})%>
            <!-- <% for(let post in pMessage){%>
                <h1>
                    <%= pMessage[post].title %>
                </h1>
                <p>
                    <%= pMessage[post].body %>
                </p>
                <%}%> -->



            <%- include('partials/footer')-%>